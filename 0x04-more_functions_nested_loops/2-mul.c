#include "main.h"

/**
 * mul - function to multiply 2 numbers.
 * @a: num1 to multiply
 * @b: num2 to multiply
 *
 * Return: multiplication of 2 numbers
*/

int mul(int a, int b)
{
	return (a * b);
}
